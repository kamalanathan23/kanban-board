# Optional: override API URL when not using Vite dev proxy
# VITE_API_BASE_URL=http://localhost:4000/api

# Vercel (frontend + backend on same project, Services preset):
# VITE_API_BASE_URL=/api

# Vercel frontend only + backend on Render/Railway:
# VITE_API_BASE_URL=https://your-api-host.com/api
